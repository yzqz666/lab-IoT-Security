#include <array>
#include <cstdint>
#include <cstring>
#include <iostream>
#include <iomanip>
#include <vector>
#include <chrono>
#include <random>
#include <sstream>

using u8 = uint8_t;
using u32 = uint32_t;

static const u8 sbox[256] = {
    
};

static const u8 inv_sbox[256] = {
    
};

static const u8 Rcon[11] = {
    0x00,
    0x01,0x02,0x04,0x08,0x10,0x20,0x40,0x80,0x1B,0x36
};

inline u8 xtime(u8 x) {
    return (u8)((x << 1) ^ ((x & 0x80) ? 0x1b : 0x00));
}

inline u8 mul(u8 a, u8 b) {
    u8 res = 0;
    while (b) {
        if (b & 1) res ^= a;
        a = xtime(a);
        b >>= 1;
    }
    return res;
}


using State = std::array<u8, 16>;

void SubBytes(State &s) {
    for (int i = 0; i < 16; ++i) s[i] = sbox[s[i]];
}
void InvSubBytes(State &s) {
    for (int i = 0; i < 16; ++i) s[i] = inv_sbox[s[i]];
}

void ShiftRows(State &s) {
    State t = s;
    t[0] = s[0]; t[4] = s[4]; t[8] = s[8]; t[12] = s[12];
    t[1] = s[5]; t[5] = s[9]; t[9] = s[13]; t[13] = s[1];
    t[2] = s[10]; t[6] = s[14]; t[10] = s[2]; t[14] = s[6];
    t[3] = s[15]; t[7] = s[3]; t[11] = s[7]; t[15] = s[11];
    s = t;
}
void InvShiftRows(State &s) {
    State t = s;
    t[0] = s[0]; t[4] = s[4]; t[8] = s[8]; t[12] = s[12];
    t[1] = s[13]; t[5] = s[1]; t[9] = s[5]; t[13] = s[9];
    t[2] = s[10]; t[6] = s[14]; t[10] = s[2]; t[14] = s[6];
    t[3] = s[7]; t[7] = s[11]; t[11] = s[15]; t[15] = s[3];
    s = t;
}

void MixColumns(State &s) {
    for (int c = 0; c < 4; ++c) {
        int i = c * 4;
        u8 a0 = s[i+0], a1 = s[i+1], a2 = s[i+2], a3 = s[i+3];
        u8 r0 = (u8)(mul(0x02, a0) ^ mul(0x03, a1) ^ a2 ^ a3);
        u8 r1 = (u8)(a0 ^ mul(0x02, a1) ^ mul(0x03, a2) ^ a3);
        u8 r2 = (u8)(a0 ^ a1 ^ mul(0x02, a2) ^ mul(0x03, a3));
        u8 r3 = (u8)(mul(0x03, a0) ^ a1 ^ a2 ^ mul(0x02, a3));
        s[i+0] = r0; s[i+1] = r1; s[i+2] = r2; s[i+3] = r3;
    }
}
void InvMixColumns(State &s) {
    for (int c = 0; c < 4; ++c) {
        int i = c * 4;
        u8 a0 = s[i+0], a1 = s[i+1], a2 = s[i+2], a3 = s[i+3];
        u8 r0 = (u8)(mul(0x0e, a0) ^ mul(0x0b, a1) ^ mul(0x0d, a2) ^ mul(0x09, a3));
        u8 r1 = (u8)(mul(0x09, a0) ^ mul(0x0e, a1) ^ mul(0x0b, a2) ^ mul(0x0d, a3));
        u8 r2 = (u8)(mul(0x0d, a0) ^ mul(0x09, a1) ^ mul(0x0e, a2) ^ mul(0x0b, a3));
        u8 r3 = (u8)(mul(0x0b, a0) ^ mul(0x0d, a1) ^ mul(0x09, a2) ^ mul(0x0e, a3));
        s[i+0] = r0; s[i+1] = r1; s[i+2] = r2; s[i+3] = r3;
    }
}

void AddRoundKey(State &s, const u8 *roundKey) {
    for (int i = 0; i < 16; ++i) s[i] ^= roundKey[i];
}

static const int Nk = 4;  
static const int Nr = 10; 
static const int Nb = 4;

void KeyExpansion(const u8 key[16], u8 roundKeys[176]) {
    std::memcpy(roundKeys, key, 16);
    int bytesGenerated = 16;
    int rconIteration = 1;
    u8 temp[4];

    while (bytesGenerated < 176) {
        for (int i = 0; i < 4; ++i)
            temp[i] = roundKeys[bytesGenerated - 4 + i];

        if (bytesGenerated % 16 == 0) {
            u8 t = temp[0];
            temp[0] = temp[1]; temp[1] = temp[2]; temp[2] = temp[3]; temp[3] = t;
            for (int i = 0; i < 4; ++i) temp[i] = sbox[temp[i]];
            temp[0] ^= Rcon[rconIteration];
            ++rconIteration;
        }

        for (int i = 0; i < 4; ++i) {
            roundKeys[bytesGenerated] = (u8)(roundKeys[bytesGenerated - 16] ^ temp[i]);
            ++bytesGenerated;
        }
    }
}

void AES_EncryptBlock(const u8 in[16], u8 out[16], const u8 roundKeys[176]) {
    State state;
    for (int i = 0; i < 16; ++i) state[i] = in[i];

    AddRoundKey(state, roundKeys); 

    for (int round = 1; round <= Nr-1; ++round) {
        SubBytes(state);
        ShiftRows(state);
        MixColumns(state);
        AddRoundKey(state, roundKeys + round * 16);
    }

    SubBytes(state);
    ShiftRows(state);
    AddRoundKey(state, roundKeys + Nr * 16);

    for (int i = 0; i < 16; ++i) out[i] = state[i];
}

void AES_DecryptBlock(const u8 in[16], u8 out[16], const u8 roundKeys[176]) {
    State state;
    for (int i = 0; i < 16; ++i) state[i] = in[i];

    AddRoundKey(state, roundKeys + Nr * 16);

    for (int round = Nr - 1; round >= 1; --round) {
        InvShiftRows(state);
        InvSubBytes(state);
        AddRoundKey(state, roundKeys + round * 16);
        InvMixColumns(state);
    }

    InvShiftRows(state);
    InvSubBytes(state);
    AddRoundKey(state, roundKeys);

    for (int i = 0; i < 16; ++i) out[i] = state[i];
}

std::vector<u8> pkcs7_pad(const std::vector<u8> &data) {
    size_t n = data.size();
    size_t pad_len = 16 - (n % 16);
    std::vector<u8> out = data;
    out.insert(out.end(), pad_len, (u8)pad_len);
    return out;
}

std::vector<u8> pkcs7_unpad(const std::vector<u8> &data) {
    if (data.empty() || (data.size() % 16) != 0) throw std::runtime_error("Invalid padded data");
    u8 pad = data.back();
    if (pad == 0 || pad > 16) throw std::runtime_error("Invalid padding");
    for (size_t i = data.size() - pad; i < data.size(); ++i)
        if (data[i] != pad) throw std::runtime_error("Invalid padding bytes");
    return std::vector<u8>(data.begin(), data.end() - pad);
}

std::vector<u8> AES_CBC_Encrypt(const std::vector<u8> &plaintext, const u8 key[16], const u8 iv[16]) {
    u8 roundKeys[176];
    KeyExpansion(key, roundKeys);

    std::vector<u8> padded = pkcs7_pad(plaintext);
    std::vector<u8> ciphertext(padded.size());

    u8 prev[16];
    std::memcpy(prev, iv, 16);

    u8 block_in[16], block_xor[16], block_out[16];
    for (size_t off = 0; off < padded.size(); off += 16) {
        for (int i = 0; i < 16; ++i) block_in[i] = padded[off + i] ^ prev[i];
        AES_EncryptBlock(block_in, block_out, roundKeys);
        for (int i = 0; i < 16; ++i) {
            ciphertext[off + i] = block_out[i];
            prev[i] = block_out[i];
        }
    }
    return ciphertext;
}

std::vector<u8> AES_CBC_Decrypt(const std::vector<u8> &ciphertext, const u8 key[16], const u8 iv[16]) {
    if (ciphertext.size() % 16 != 0) throw std::runtime_error("ciphertext not block aligned");
    u8 roundKeys[176];
    KeyExpansion(key, roundKeys);

    std::vector<u8> plaintext(ciphertext.size());

    u8 prev[16];
    std::memcpy(prev, iv, 16);

    u8 block_out[16], block_in[16];
    for (size_t off = 0; off < ciphertext.size(); off += 16) {
        for (int i = 0; i < 16; ++i) block_in[i] = ciphertext[off + i];
        AES_DecryptBlock(block_in, block_out, roundKeys);
        for (int i = 0; i < 16; ++i) {
            plaintext[off + i] = block_out[i] ^ prev[i];
            prev[i] = block_in[i];
        }
    }
    return pkcs7_unpad(plaintext);
}

std::string to_hex(const std::vector<u8> &v) {
    std::ostringstream oss;
    oss << std::hex << std::setfill('0');
    for (u8 b : v) oss << std::setw(2) << (int)b;
    return oss.str();
}

std::array<u8,16> rand_bytes() {
    std::random_device rd;
    std::mt19937_64 gen(rd());
    std::uniform_int_distribution<int> dist(0,255);
    std::array<u8,16> a;
    for (int i=0;i<16;++i) a[i] = (u8)dist(gen);
    return a;
}

int main() {
    const char *k = "Thats my Kung Fu"; 
    u8 key[16];
    std::memcpy(key, k, 16);

    auto iv_arr = rand_bytes();
    u8 iv[16];
    std::memcpy(iv, iv_arr.data(), 16);

    std::string plain_text = "nihao";
    std::cout << "原始文本: " << plain_text << "\n";
    std::vector<u8> plaintext(plain_text.begin(), plain_text.end());

    // 加密
    std::vector<u8> ciphertext = AES_CBC_Encrypt(plaintext, key, iv);
    std::cout << "加密后文本:" <<  to_hex(ciphertext) << "\n";

    // 解密
    std::vector<u8> decrypted = AES_CBC_Decrypt(ciphertext, key, iv);
    std::string recovered(decrypted.begin(), decrypted.end());
    std::cout << "解密文本:" << recovered << "\n";

    return 0;
}